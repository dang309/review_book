import styled from 'styled-components';

export default styled.div`
  margin-bottom: 8px;

  display: flex;
  align-items: center;
  gap: 8px;

  .home {
    a {
      color: #0f52ba;
      outline: none;
    }
  }

  .slash,
  .book_name {
    color: rgba(0, 0, 0, 0.8);
  }
`;
